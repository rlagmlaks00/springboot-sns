spring:
  application:
    name: springboot-sns
  datasource:
    url: jdbc:h2:mem:snsdb
    driver-class-name: org.h2.Driver
    username: sa
    password: null
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
